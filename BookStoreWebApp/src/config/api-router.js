const baseRouter = "/api";
const googleRouter = "/google-api";

export const ApiRouter = {
    BOOKSGOOGLEAPI: `${googleRouter}/books/v1`,
    VENDOR_DELIVERIES: `${baseRouter}/VendorDelivories`,
    BOOKS: `${baseRouter}/books`
}